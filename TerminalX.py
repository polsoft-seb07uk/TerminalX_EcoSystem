"""TerminalX bootstrapper.

This file starts the integrated core package as a single terminal organism.
"""

from core import TerminalX


if __name__ == "__main__":
    terminal = TerminalX()
    terminal.run()
